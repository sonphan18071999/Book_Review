import { TextSpacingDirective } from './text-spacing.directive';

describe('TextSpacingDirective', () => {
  it('should create an instance', () => {
    const directive = new TextSpacingDirective();
    expect(directive).toBeTruthy();
  });
});
